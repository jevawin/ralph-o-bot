# Designer Role Support

**Status:** Backlog — depends on Clancy designer role shipping first

Clancy will soon have a designer role, which will introduce design tickets into the pipeline with a lot more back-and-forth than typical plan/build tickets.

## What's Needed

Ralph will need to handle a design stage — likely a new label (e.g. `clancy:design`) and comment cycle similar to brief/plan but potentially with more iterations. The review loop may also need to accommodate design feedback differently from code feedback.

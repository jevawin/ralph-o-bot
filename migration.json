{
  "version": "0.4.6",
  "clancyVersion": "0.8.11",
  "notes": "Background daemon — start and restart no longer block the terminal.",
  "fixes": ["ralph-o-bot start now runs the daemon in the background and returns control to the terminal. Logs go to logs/ralph.log.", "ralph-o-bot update restart now uses systemctl when available, so it no longer hijacks the terminal."],
  "boardChanges": [],
  "requiresBoot": false,
  "requiresManual": false
}
